# ---------------------------------------------------------------------------
# External API (Railway)
# ---------------------------------------------------------------------------
# Base URL of the deployed backend. The admin panel proxies all data through
# /api/admin/* on this host. Do NOT include a trailing slash.
NEXT_PUBLIC_API_URL=https://omni-shift-ai-api-production.up.railway.app
